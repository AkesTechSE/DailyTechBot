version: '3.8'

services:
  dailytechbot:
    build: .
    container_name: dailytechbot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - TZ=UTC
    volumes:
      - ./logs:/app/logs

